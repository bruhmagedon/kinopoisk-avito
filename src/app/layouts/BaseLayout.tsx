import styles from './BaseLayout.module.scss';

const BaseLayout = () => {
   return (
      <div className={styles.layout}>
         BaseLayout
         {/* <Header /> */}
         {/* <LeftSidebar />
      <Outlet />
      <RightSidebar /> */}
      </div>
   );
};

export default BaseLayout;
